class Player:
    def __init__(self, name, hp):
        self.name = name
        self.hp = hp
        inventory = []
        target_name = ""
        target_num = ""
        
    def is_alive(self):
        return self.hp > 0
    
    def is_targeting(Enemy):
        if target_name == Enemy.name and target_num == Enemy.num:
            return True
        else:
            return False

    def target(Enemy):
    
    def print_inventory(self):
        for item in self.inventory:
            print(item, '\n')
    
